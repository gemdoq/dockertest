{{>layouts/header}}

{{#hospital}}
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h6 class="card-subtitle mb-2 text-muted">{{id}}</h6>
            <h5 class="card-title">{{hospital_name}}</h5>
            <p class="card-text">{{road_name_address}}</p>
            <!--            <a href="/articles/{{id}}/toedit" class="btn btn-primary">글 수정</a>-->
            <!--            <a href="/articles/{{id}}/delete" class="btn btn-danger">글 삭제</a>-->
        </div>
    </div>
{{/hospital}}
<!--<a href="/articles/list" class="btn btn-primary">글 목록</a>-->


{{>layouts/footer}}